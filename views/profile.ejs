<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to right, #6a11cb, #2575fc);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .profile-container {
            background-color: #fff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            width: 450px;
            max-width: 100%;
            text-align: center;
            transition: transform 0.3s;
        }

        .profile-container:hover {
            transform: translateY(-10px);
        }

        .profile-photo {
            border-radius: 50%;
            width: 150px;
            height: 150px;
            object-fit: cover;
            margin-bottom: 20px;
            border: 5px solid #6a11cb;
            transition: box-shadow 0.3s;
        }

        .profile-photo:hover {
            box-shadow: 0 0 15px rgba(106, 17, 203, 0.8);
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
            font-size: 2rem;
            letter-spacing: 1px;
        }

        p {
            font-size: 18px;
            margin: 10px 0;
            color: #555;
            font-weight: bold;
        }

        .profile-info {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 10px;
        }

        .profile-info p {
            margin: 5px 0;
            color: #444;
            font-size: 1rem;
        }

        .profile-info p span {
            color: #6a11cb;
            font-weight: 500;
        }

        @media (max-width: 600px) {
            .profile-container {
                width: 90%;
                padding: 20px;
            }

            h1 {
                font-size: 1.5rem;
            }

            p {
                font-size: 16px;
            }

            .profile-photo {
                width: 120px;
                height: 120px;
            }
        }
    </style>
</head>
<body>

<div class="profile-container">
    <h1>Profile Dashboard</h1>
    <img src="<%= profilePhoto %>.png" alt="Profile Photo" class="profile-photo">
    
    <div class="profile-info">
        <p><span>Name:</span> <%= name %></p>
        <p><span>Email:</span> <%= email %></p>
        <p><span>Phone:</span> <%= phone %></p>
        <p><span>Gender:</span> <%= gender %></p>
        <p><span>Address:</span> <%= address %></p>
        <p><span>UserType</span> <%= userType %></p>
        <button style="margin-left: 2px;" onclick="window.location.href='/loginPage'">Login Page</button>
    </div>
</div>

</body>
</html>
